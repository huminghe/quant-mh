# Lessons Learned

Claude 在此项目中被纠正的错误和对应规则。每次被纠正时自动追加，不需要用户提醒。

交易规范（复权方式、T+1、回测验收标准等）在 `.claude/rules/trading-standards.md`，不在此文件重复。

---

- [archive 归档标准]："决策已执行"不能作为归档理由。执行了的决策文档记录的是"为什么这样做"，是当前配置的依据，恰恰需要保留在主路径。归档条件是：内容已被其他文档完全吸收 AND 原文档不再有独立参考价值。用户说"可以先做 X 再做 Y"，不代表"允许跳过 X"，而是"先做 X，再做 Y"的顺序指令。有歧义时先确认，不要默认跳过。
- [规则提炼]：完成非 trivial 任务（文件整理、架构决策、调研结论）后，不能只做事不总结。做完后要主动把确定的规则写进 CLAUDE.md 或 memory，并询问用户是否需要固化。否则下次遇到同类情况还是会走老路。
- [数据合并时区]：TV 导出交易记录是 UTC+8 naive datetime，Binance API 是 UTC。合并前必须先将 TV 时间 -8h 转 UTC，否则等效于用了未来 8 小时数据。这是系统性错误，会导致所有正向结论都是假的。
- [正向结论要更怀疑]：在量化验证中，某个过滤器突然出现正向结论时，应先检查数据管道（时区、lookahead、数据对齐），而不是直接相信结论。负向结论通常更可信，正向结论更可能是数据泄漏。
- [总结检查清单要在 compact 前完成]：CLAUDE.md 里的"研究总结检查清单"（4项）必须在 compact 前全部完成。compact 会打断流程，导致下次会话需要补做。
- [入场过滤器是结构性死路]：对低胜率+高盈亏比的趋势跟踪策略，任何确认类信号系统性滞后（盈利集中在趋势启动前1-3根bar）。遇到类似策略时，直接指向出场优化和仓位管理。详见 `crypto/docs/active/validation_results.md`。
- [ATR 定仓/出场/仓位管理已全部穷尽]：ATR归一化定仓已验证最优，CE已上线，所有其余方向（Kelly/非对称/信号加权/ADX出场/KAMA等）全部无效。详见 `crypto/docs/active/validation_results.md` 和 `crypto/docs/background/trend_following_research.md`。
- [xlsx 列名匹配要用模糊查找]：TV导出xlsx的价格列名是"价格 USDT"不是"价格"，用 `col_idx.get('价格', -1)` 会取到-1，导致取到最后一列（累计P&L%）。正确做法：`next((k for k in col_idx if '价格' in k or k == 'Price'), None)`。
- [差值大小过滤等同于入场过滤器]：加"差值 > k×ATR 才入场"本质和入场过滤器一样，4标的实测 k 越大 Sharpe 越低。详见 `crypto/docs/active/validation_results.md`。
- [调研预估不等于已验证]：之前将 CE 列为"预估不值得验证"，实际回测后 IS+OOS 均正向。文献调研给出的方向判断是概率性的，"预估无效"不等于"已验证无效"，有实际数据时以回测结果为准。
- [TV 交易记录方向字段在"信号"列而非"类型"列]：TV 导出 xlsx 中，"类型"列值为"多头进场"/"空头进场"（中文），"信号"列值为"long"/"short"（英文小写）。读取方向时应优先用"信号"列的 'long'/'short'，不要只依赖"类型"列的中文关键词匹配。
- [信号强度加权仓位是软版入场过滤器]："强信号满仓、弱信号缩仓"方向本质与入场过滤器相同，已验证无效。详见 `crypto/docs/active/validation_results.md`。
- [资金费率过滤需区分"泛化双向"和"单向针对性"]：泛化双向过滤（-6.4%）无效；FR_MA7 > 0 才允许做空有效（IS+0.132/OOS+0.511）。两者不冲突：后者利用了 FR<0 时做空付费+逼空风险的非对称信息。详见 `crypto/docs/active/validation_results.md`。
- [目录结构调整后要全量扫描引用路径]：docs/ 目录结构变更（如新增 active/background/archive 子目录）后，必须检查所有 .md 文件的交叉引用，范围包括：HANDOFF.md、CLAUDE.md（各层级）、lessons.md、skills 文件、docs/ 内部文件——不只是被移动的文件本身。遗漏会导致路径失效或状态过时，本次 health_monitor.md 和 HANDOFF.md 均因此出现问题。
- [TV xlsx 格式会静默变更]：TradingView 会不定期更新导出格式（如 sheet 名 `交易清单`→`交易`，字段名变更）且不发通知。每次遇到新 xlsx 报错时，先用 `wb.sheetnames` 和打印表头确认实际格式，再修代码。分析脚本中所有 sheet 名和字段名读取都应做 fallback 兼容，不要硬编码单个名称。
- [多ETF矩阵末尾NaN导致净值假性暴跌]：不同ETF数据更新日期不同步时（如新加入的ETF比其余ETF多几天数据），`load_close_matrix` 末尾会出现大量NaN行。回测循环遍历到这些行时，持仓标的价格全为NaN，净值"假性暴跌"至仅剩现金。修复：`load_close_matrix` 截断到超过半数标的有数据的最后一天（已在 `fetch_data.py` 实现）。新增任何ETF数据源时都要检查日期对齐。
- [动量轮动策略加入新标的要先验证信号质量]：加入相关性低的资产（商品ETF）直觉上是分散化，但在Top-N动量轮动里，新标的直接参与竞争坑位。若新标的动量信号质量不如原有标的（IC低、趋势持续性差），反而稀释组合动量强度，实测净负效果。正确流程：先单独测试新标的的动量信号历史表现，再决定是否加入标的池。
- [子agent代码分析结论要手动验证]：子agent分析代码时可能误判切片逻辑。`series.iloc[i-window:i]` 不含 `i`，子agent却报告为"使用当日数据，存在前视偏差"。代码分析子agent的结论要用简短脚本验证，不要直接相信并动手改代码。


- [ETF轮动拥挤度信号"有效"结论已被推翻，见下方2026-07-08条目]：脏数据（含13处复权断层）上测得threshold=0.75, factor=0.2时夏普0.83→1.005，69%参数网格超基线，一度推翻"优化已穷尽"的结论；但换成干净数据后边际收益归零（+0.007，不显著），已从上线信号移除，当前配置是纯风险调整动量（无拥挤度）。保留此条仅作历史记录，具体原因见下方"已验证有效"条目。
- ["优化已穷尽"结论本身可被推翻]：写下"所有方向已穷尽"时，前提是"当前数据依赖下"。若数据可用性发生变化（如用价格代理绕开权限），或出现从未测试过的信号类别（如拥挤度），结论需要重新验证。不要用"穷尽"作为拒绝新方向的理由，正确判断标准是"此方向是否有逻辑支撑 + 是否经过回测"。
- [价格相关性是拥挤度的低成本代理]：拥挤度原本需要成分股持仓数据（无权限），改用ETF间滚动相关系数（60日均值→252日历史分位）效果相同甚至更好，且无数据依赖。同类问题：若目标指标无法直接获取，优先找价格层面的代理变量，不要因数据缺失直接放弃方向。
- [北向资金信号2022后已失效]：北向数据披露规则2022年后变化，信号IC从+0.015衰减至-0.178（反向），方向胜率从51.6%降至42.8%。用北向作为行业ETF轮动过滤器会降低策略表现，不可用。
- [PE高分位≠A股熊市]：用PE历史分位数定义市场状态在A股反向——PE高分位往往对应行情最强的科技/成长牛市（如2017/2020/2025年）。任何以"高估值=减仓"为逻辑的区制切换在A股都会系统性错过最大涨幅，等效于动态持仓数的失效原因。
- [表面最优配置要诊断来源]：方向A+D全样本夏普0.98看似最优，但诊断发现最长禁买段2024-08起长达693天，OOS超额来自"碰巧不操作"而非信号有效性。出现"IS期表现不错+OOS大幅超越"模式时，先检查是否有某个外生事件恰好覆盖了OOS区间。
- [A股ETF动量不同于全球多资产TAA]：Dual Momentum、skip-month、动态持仓数等在全球多资产（股/债/商品）文献中有效的改进，在A股ETF月度轮换中全部无效甚至有害。核心原因：A股行业ETF的高波动期 = 趋势最强期（如2019/2020/2024年科技行情），任何降仓机制都会错过最大涨幅。A股ETF动量来源是机构资金惯性，而非全球大类资产的宏观驱动，适用逻辑不同。
- [文献结论不等于A股结论]：国际量化文献（Antonacci/Faber/Keller/Moskowitz）的策略改进方向，移植到A股ETF月度轮换前必须回测验证，不能直接相信。A股市场结构（散户主导/政策市/T+1/无做空）与全球成熟市场差异巨大，导致很多"普遍有效"的改进在A股反向。
- [文档收尾要做全量扫描，不只改内容文件]：每次结论有实质变化，需要同步更新的地方通常有4处：① 内容文件本身（research.md / docs）② memory topic 文件（project_*.md）③ MEMORY.md 索引描述行 ④ 内容文件顶部的"当前上线结果/当前状态"摘要行。只改其中一处会留下彼此矛盾的过时快照，导致下次对话读到错误的状态。
- [ETF场内成交量不适用量价分析]：A股ETF场内成交量包含大量套利交易（折溢价套利者在折溢价时机械买卖，与行业趋势无关），月均IC=0.015，IC>0占比49.2%（随机水平）。量价确认逻辑在个股层面有效，但在ETF场内成交层面信噪比过低，不适用。遇到ETF层面的量价信号方向时，直接排除，不需要回测。
- [标的池高相关≠重复品种，去重必须用指数定义]：A股牛市行情会让所有ETF相关性虚高（如芯片ETF vs 农业ETF相关性0.997），导致相关性阈值过滤产生大量假阳性。ETF去重的正确方法是检查追踪指数是否相同，而非用价格相关性。真正的重复品种（如512100和159632同追中证1000）通过指数定义即可识别，45只ETF池中实际只有1对真正重复。
- [系统性信号会覆盖手工组合约束]：当策略已有动态信号（如拥挤度软过滤）在信号层做跨标的压制时，再叠加手工规则（如子行业组内限制2席）通常是冗余的——动态信号已经自动降低了高同质性标的的得分，手工约束不会产生增量效果。判断标准：若已有信号和新约束针对的是同一个风险来源（行业集中），优先验证已有信号是否已覆盖，再决定是否额外加约束。
- [重复问"还有什么要更新"是验收完成的信号]：用户连续追问"还有什么需要更新/总结"，且每次检查后都没发现实质遗漏，说明本轮工作已真正收尾。第三次问出来时，直接确认"已完整，无遗漏"，不要再做虚假的扫描。区别：第一次问是检查习惯（正确），第二次发现细节（正确），第三次是确认（应该终止）。
- [宏观和资金流信号在A股ETF轮动中无效]：社融IC验证弱有效(0.122)，但全量回测叠加后0/6参数超过拥挤度基线，因为社融横截面压制和A股高波动=趋势最强期的机制冲突，弱月正好可能是趋势行情启动时。ETF资金净流量反向IC验证有效(-0.062)，但全量回测0/11参数超过基线，最优0.904 vs 基线1.005，原因是资金流信号与动量得分已有很强相关（净流出往往是滞后的，趋势已经反转时才净流出）。两类信号都不能提升A股ETF月度轮动表现。
- [A股ETF月度动量信号不可削弱]：Shrinkage（截面均值收缩）、惰性调仓（持仓排名小幅下滑时不换仓）、信号EMA平滑（跨期平滑月度得分）、拥挤度衰减加权（EWM替代等权）全部有害（Δ夏普-0.15~-0.59）。根本原因：A股ETF月度动量的alpha来源是机构资金惯性，信号区分度本已接近极限，任何降低区分度（收缩/平滑）或降低响应速度（惰性调仓/平滑）的操作均净负。遇到类似"降噪/稳健性"类改进方向时，先想这个机制是否与机构惯性逻辑冲突。
- [A股月末价格索引要用实际交易日]：`resample("ME").last().index` 生成的是自然月末（如2016-04-30），不一定是交易日。用 `.loc[自然月末]` 直接索引会 KeyError。正确做法：用 `close_panel.index[close_panel.index <= m][-1]` 取 <= 自然月末的最后一个实际交易日，再用该日期索引面板。
- [文献因子IC与实测差距要预先说明]：文献报告的因子ICIR（如反转因子 -0.244）通常来自全A股含小盘股，限定沪深300/中证500后信号会系统性更弱（实测约 -0.10）。遇到文献因子时，先说明"这是全A股结果，沪深300/中证500可能打折"，不要直接用文献数字作为实测预期。
- [低波动因子在A股选股中是非对称因子]：截面IC验证（ICIR 0.40）看似显著，但选股回测跑输基准3%/年。根本原因：低波动溢价在A股牛市（2019-2021）反向，高波动成长股才是最大涨幅来源。IC验证的全样本均值被熊市/震荡市年份拉高，掩盖了牛市反向的风险。做A股选股因子验证时，除看全样本ICIR外，必须分年度检查IC方向的稳定性，尤其是牛市年份。
- [行业内因子排名能提升信号质量]：对EP做行业内截面排名（而非全截面），ICIR从+0.255提升至+0.321，且在2019-2022成长牛市期间维持正向（全截面EP在行情强的成长行业系统性失效）。遇到价值类因子在成长牛市反向时，优先尝试行业中性化，而非放弃该因子。
- [盈利增速稳定性是A股选股的鲁棒因子]：净利润YoY增速的历史标准差（8季度）取负，ICIR +0.322，2016-2026全部年度均维持正IC（含2019-2022成长牛市）。机制：盈利稳定不依赖"便宜"或"低波动"，是公司质量的直接度量，对A股市场状态切换不敏感。遇到价值/低波动因子牛市失效时，盈利稳定性是第一候选替代方向。
- [IC好+选股差的诊断标准路径]：因子截面IC显著但选股回测跑输基准时，标准诊断顺序：① 看年度IC分拆（发现某几年系统性反向）→ ② 找反向年份的市场状态（牛市/熊市/成长行情）→ ③ 分析因子经济学逻辑是否与该状态冲突 → ④ 决定是换因子还是加市场状态条件。不要在没诊断前直接加更多因子。
- [因子数量少时LGB不优于线性加权]：因子数量<10且截面样本有限（500只×125月）时，LightGBM无法学到有效非线性交互，结果与线性加权持平或更差。LGB适合因子数量扩充到15+个后再验证，不是少因子阶段的优化手段。
- [自适应权重IS好/OOS差=过拟合信号]：滚动IC加权、硬切换等自适应权重机制，若IS改善幅度越大则OOS越差，是过拟合的典型特征。根本原因：A股风格切换剧烈且不可预测，基于历史IC的自适应权重必然滞后1期以上。遇到IS/OOS差距随IS改善而扩大的模式，直接判定过拟合。
- [分析师预期修正因子需先查覆盖率]：tushare report_rc 中证500覆盖率仅28.6%（一个月约143只），不足以做截面选股。使用任何外部数据源构造因子前，先验证在目标指数内的覆盖率，低于60%时直接放弃，不要花时间下载和构造信号。
- [换指数不能解决因子在牛市反向的问题]：沪深300 V2全样本超额-2.1%，比中证500（-1.5%）更差，同样2019起系统性反向。大盘股成长行情更集中，反而放大了因子失效。遇到"换指数改善因子有效性"方向时，先问：失效的根本原因是行业暴露还是市值暴露？若是A股风格切换（成长/价值），换指数不会有效。
- [行业名称体系要核对来源，不能假设一致]：`etf_stock_hybrid_backtest.py` 用申万行业名（"电子"/"计算机"/"电力设备"）做 ETF→行业映射，但误用了 `tushare stock_basic.industry`（证监会旧分类，110个细分行业，"半导体"/"全国地产"）做实际匹配，两套词表仅"银行"偶然重合，导致29个行业ETF槽位中28个查无候选股票直接降级为ETF直持，回测跑出"超额恰好为0.0%"这种过于工整的数字——这是识别此类bug的信号。正确数据源是 `pro.index_classify(src='SW2021')` + `pro.index_member_all()`（已缓存至 `a_stock/data/stock_sw_industry.parquet`）。任何"按行业名做跨表匹配/连接"的代码，写之前先打印两侧数据源的行业名集合核对是否为同一命名体系，不要假设字符串相同。**区分风险场景**：跨表按名字匹配（如ETF行业名↔股票行业名）对分类体系敏感，必须核对；组内相对排序（如"行业内EP排名"只需分组关系，不比较组名字符串）对分类体系粒度不敏感，用110类细分或31类粗分都不影响IC有效性，只是文档标注要写实际用的分类体系，不能想当然写"申万"。
- [技术决策变更要回写规范文档，不能只留在memory里]：71天前vectorbt因LLVM依赖冲突装不上，memory记录"短期自实现回测"，但这条临时决定从未同步回 `a_stock/CLAUDE.md` 和 `strategy_notes.md`，导致两份文档一直写着"vectorbt主力"，而实际27个回测脚本全部是自实现pandas方案，文档和代码脱节两个多月。判断标准：只要决定会长期生效（不是"下次再评估"的临时权宜），就要在做出决定的同一次会话里同步回写规范文档，不能只留memory快照等下次偶然发现再补。
- [memory 内容误写进项目目录会长期存活成为重复过时源]：发现 `docs/project_index_enhancement.md` 带着完整的memory frontmatter（name/description/type）却存在于项目目录而非memory目录，内容是2026-07-03的过时快照，与同名memory文件完全重复，违反了file-boundaries.md"跨会话研究结论只能存在于memory/"的规则。这类文件不会主动暴露，只有偶然翻目录才发现。定期扫描项目内是否有带memory frontmatter格式但路径不在memory目录下的.md文件，发现即删除（内容已被正确位置的memory版本覆盖）。
- [tushare ETF后复权接口不可信，必须手工复权]：`pro_bar(asset="FD", adj="hfq")` 对ETF不处理份额折算（拆分/合并）事件的复权修正，会在事件发生日产生价格断层（实测最大-90%）。正确做法：拉取 `adj=None` 不复权价 + `fund_adj` 复权因子，手工 `价格 × adj_factor` 还原。任何用 tushare ETF数据做复权序列的场景都要用这个方案，不能信任接口自带的 `adj="hfq"`。
- [tushare fund_adj 单次调用有隐藏分页上限]：`fund_adj` 单次调用静默截断到约2000条，不报错、不提示。长区间（如2015年至今约2800个交易日）请求会只返回最近2000条，早期日期完全没有 `adj_factor`，若代码对缺失值做 `fillna(1.0)` 会导致早期价格漏复权、在截断点产生新断层。正确做法：按≤2年为一段分批拉取再拼接（`fetch_fund_adj_full()`，见 `a_stock/data/fetch_data.py`）。遇到任何"长区间历史数据用起来正常，但发现某个日期附近数据突变"时，先怀疑是否有类似的隐藏分页/截断。
- [增量更新对"后复权"类数据是系统性风险，全量重拉更可靠]：后复权价格的复权基准会随每次分红/拆分变化，增量追加新数据到旧文件时，旧数据的复权基准是"截止上次更新时"的，新拉取部分是"当前最新"的，两者拼接必然在拼接点产生断层。ETF/股票复权价数据不应做增量更新，每次运行全量重新拉取覆盖，虽然多耗时但避免了这类隐蔽bug。`fetch_data.py` 已改为全量重拉模式。
- ["已验证有效"的结论要检查数据健全性，不能只看回测数字合理]：ETF轮动"行业拥挤度过滤+0.177夏普"这个结论在脏数据（含13处复权断层）上通过了参数网格扫描（69%配置超基线）和IS/OOS验证，看起来非常扎实，但换成干净数据后边际收益归零（+0.007）。数字断层往往表现为"极端单日暴涨暴跌"，恰好会被某些过滤器/信号"利用"产生虚假的正向效果，且不会被常规的IS/OOS检验发现（断层在训练集和测试集里都存在，不会体现为过拟合特征）。定期（尤其是发布上线前）对原始价格数据做健全性扫描（如±15%/20%单日涨跌幅扫描），不要只信任"跑出来的数字合理"。
- [结论变化后要扫描"引用该结论的上游文档"，不只是"记录该结论的文档"]：更新ETF轮动新结论时，第一轮只改了 research.md/ETF轮动调研.md/memory/MEMORY.md（记录结论本身的文档），漏了三类引用方：① `.claude/rules/trading-standards.md`（权威规范，"A股复权默认hfq"没区分ETF场景，此次bug恰恰是ETF专属）② `docs/superpowers/specs/*.md`（历史设计文档引用旧夏普数值作基准，未标注过时）③ 项目总览 `a_stock/CLAUDE.md` 和 memory `project_a_stock_research.md`（策略方向表格仍把已关闭的"指数增强"列为生效方向）。判断标准：grep关键数字/结论词（如"1.005"、"拥挤度"）要扫全项目而不是只扫`docs/`目录，规范文件（rules/）、历史spec、项目总览CLAUDE.md都要覆盖。
- [终结性状态描述来自我此前生成的memory时，用前先向用户确认]：上一条"扫描引用方"修复时，直接照搬了memory里"指数增强已关闭"的措辞，扩散写入了9个文件（research.md、CLAUDE.md、HANDOFF.md、多个memory文件），但用户随后纠正："只是遇到困难，方向还要继续做"。根源：memory记录的是我自己此前某次会话的判断，不是用户当次确认的事实，我在没有跟用户复核的情况下把它当作既定结论使用并传播。规则：遇到"已关闭/停止/放弃/不再投入"这类终结性状态词时，如果信息来源是我自己生成的旧memory/文档而非用户在当前对话中的确认，使用前先跟用户确认一句（"XX 目前还是关闭状态吗？"），不要直接采信并往下游文档扩散。方向性/框架性结论（"某类方法无效"）风险较低，可以直接用；但"整个策略方向是否继续投入"这种业务决策必须用户拍板，不是纯技术结论。
- [脏数据修复后，正向反转结论要比负向结论用更严格的验证标准]：ETF轮动Ledoit-Wolf最小方差权重方向，脏数据判"负效果"(0.67<基线0.83)，修复复权bug后用同一单点参数重跑变成"有效"(1.13>基线1.05)。若止步于此就会得出错误的"采用"结论。补做参数网格扫描/逐年对比/滚动窗口/IS-OOS四类验证后发现：仅42%参数超基线（孤立尖峰非系统性）、改善集中在单一年份（2019年+22.1%）、OOS夏普几乎不变（1.917→1.908，样本内提升样本外无增量，典型过拟合）。规则：脏数据修复后若某方向由"无效"反转为"有效"，必须补做完整稳健性验证（不能只信一次单点重跑的数字），验证标准应不低于当年验证"拥挤度过滤"时的四步法（网格扫描超基线比例、逐年拆分、滚动窗口、IS/OOS）。详见 `a_stock/docs/research.md` 第三轮方向A复核、`a_stock/backtest/etf_rotation_v3c_lw_fullval.py`。


- [标的池代码变更要同步排除文档，否则"未测"记录会长期误导]：QDII跨境ETF（纳指/标普500/恒生等6只）在某次会话被加入 `etf_universe.py` 标的池并上线，但 `ETF轮动调研.md` 的"不建议纳入轮动坑位"表格从未同步更新，仍写"未测（排除）"，导致后续40+种方向的调研规划里一直把QDII当作"未探索方向"排队，直到第九轮才发现它早已在跑。判断标准：给标的池/信号池增删标的这类会改变"哪些方向已测过"这个事实基础的代码变更，必须在同一次会话里同步更新调研文档里对应的"未测/排除"标记，否则文档会产生"实际已上线的东西被记录为尚未验证"的系统性错误。
- [memory 文件里的路径引用会在目录重组后集体过时，需要专项扫描]：crypto/docs/ 在某次会话从扁平结构改为 active/background/archive 三个子目录（如 strategy_versions.md → active/strategy_versions.md），当时只更新了 CLAUDE.md 和 skills，但 memory/ 下 8 个 project_*.md 文件（project_multi_strategy_switch、project_crypto_pause_mechanism、project_dual_ma_research、project_small_token_research、project_token_selection、project_crypto_analysis_workflow、project_portfolio_research_2026、project_crypto_volume_research）里"详见 crypto/docs/xxx.md"这类指针全部漏改，指向了不存在的旧路径，持续了一个多月未被发现。这类文件不会主动报错（用户不会真的去点md链接验证），只有偶然巡检才发现。判断标准：任何docs/目录结构调整，除了lessons.md #22列出的四类下游文档，还必须专项 `grep -rn` memory/ 目录下所有 `.md` 文件里的路径引用（用 `grep -oE '`[a-zA-Z0-9_/.\\-]+\\.(md|py)`'` 提取后逐一核实文件是否存在），因为 memory 文件不在项目目录里，日常 grep 项目代码时不会带出它们。
- [技术方案切换后，用旧方案生成的演示/脚手架代码要主动排查，不会自己报错]：`a_stock/strategies/reversal_20d_hs300.py` 等4个文件是2026-04-27用 paper-to-vectorbt skill 生成的演示代码（`import vectorbt as vbt`），但 vectorbt 从未装成，这组代码从生成起就没跑通过。后续该因子的实际验证走了完全不同的路径（自实现 pandas 的 `factor_ic_reversal.py`），但没人去清理前者，两个多月里死代码和真实结论并存在同一个 strategies/ 目录下，容易让后来者误以为前者是"能用的实现"。判断标准：任何"决定放弃工具/方案X"的时刻（如 vectorbt 装不上），除了同步文档描述，还要顺手 `grep` 一遍代码里是否有已生成但从未跑通、且后续被替代方案覆盖的脚手架/演示代码，移到对应的 archive/ 目录，不要留在主目录里长期误导。
- [CPCV/PBO类过拟合检验前要确认候选是否同属一个决策，不能跨方向合并]：ETF轮动第十轮把59条净值序列（覆盖10个历史脚本、含调仓频率/是否含QDII/协方差收缩/波动率目标/拥挤度过滤等5个互不相关的独立决策）直接拼成矩阵喂给`probability_of_backtest_overfitting`，得到PBO=0.533，看起来是个合理的中间数值，但用户追问"样本内表现好的那批具体是什么"后，逐路径复现才发现15条CPCV路径里"IS-best"每次都是完全不同性质的候选（一次是波动率目标参数，下一次是调仓频率）——这些候选根本不是在回答同一个问题，复合PBO没有现实含义。CPCV的前提是候选集合来自同一次参数搜索（回答同一个"该怎么调这个参数"的决策），不是"历史上所有跑过的回测"的大杂烩。改为对波动率目标网格（13候选）和拥挤度过滤网格（7候选）分别单独计算，PBO变为0.733，且这两个网格本身不含当前上线策略的核心决策，因此对"当前策略是否过拟合"反而没有直接证据，只能交叉验证已被否决的方向。判断标准：做PBO/CPCV前先问"这些候选是不是同一次网格搜索的格点"，如果是跨轮次/跨脚本汇总的历史结果，必须按决策拆开分别检验；候选数<7时CPCV统计意义也不足，不要硬跑。
- [参数网格搜索用的评分公式要跟线上实际评分公式核对一致，否则"最优参数"结论名不副实]：`etf_rotation_analysis.py`的原始Top N×窗口12组网格搜索用未风险调整的动量分数选出"Top3+25日窗口"，风险调整（`risk_adj=True`）只在选定`best_window`之后追加应用于这一个单点参数，从未针对"风险调整动量"这个真正上线的评分公式独立重跑过网格——即当前线上核心参数其实是借用了另一个评分公式选出的结果，自己的评分公式没有被网格验证过。后来用`etf_rotation_v12_riskadj_grid_pbo.py`补跑才确认Top3+25日在风险调整评分下依然最优（夏普1.053一致），侥幸没有出问题，但这是运气不是必然。判断标准：报告"网格搜索得出最优参数X"时，要核对该网格里评分/信号计算函数的参数是否与线上实际使用的完全一致（尤其是risk_adj/skip_days这类默认值为False的可选开关），如果流程是"先用简化版本选参数、再给选定参数加复杂逻辑"，必须在文档中明确标注这一点，不要让读者以为最优参数是针对最终评分公式验证出来的。
- [tushare report_rc接口限频比历史记录更严格，用前要重新实测]：第四轮记录"分钟级/8小时下载成本"，第十二轮实测发现实际限频是10次/天量级，拉取2016-2026完整历史需12天以上，数据接入成本远超预期，直接导致"分析师预期修正广度"候选被排除。判断标准：任何外部数据接口的限频记录都有失效风险（接口方可能调整策略），用前先拿小样本实测当前实际限频（连续请求几次看多久报错/限流），不要直接沿用历史某次记录的限频数字估算下载周期。
- [个体信号弱不代表集成无价值，组合层面才是最终判据]：ETF轮动第十二轮ML弱信号集成（拥挤度+成交量确认+资金流三个历史已知的弱信号做横截面排名等权集成），集成后IC（+0.038）反而低于其中最强的单一信号资金流（+0.063），但组合层面回测里资金流单独使用是负贡献（夏普0.799<基线1.019），集成后是正贡献（夏普1.235>基线，滚动2年窗口劣于基线占比仅18.9%）。判断标准：多个弱信号集成后IC看起来"变弱"不能作为否决集成方向的依据，必须做组合层面回测才能判断集成本身是否创造了增量价值——这与个体因子选择的逻辑不同，不能套用"选IC最强的那个"的直觉。详见`a_stock/docs/research.md`第十二轮小节、`etf_rotation_v15_weak_signal_ensemble.py`/`etf_rotation_v15_weak_signal_ensemble_backtest.py`。
- ["历史上验证过的弱信号"不是入选集成候选池的充分理由，要先系统扫描证明没漏掉更好的]：第十二轮选"拥挤度+成交量确认+资金流"三个信号做集成时，最初解释是"这些是历史已验证的弱信号"，被用户追问后核对发现这三个信号单独测试的IC和年度同向占比其实跟被排除的偏度因子/风格轮动一样不达标，选择过程实际是"凭记忆想起哪几个可用的就用哪几个"，不是系统扫描全部历史候选后筛出来的。用Explore子agent重新扫描12轮全部信号、按"结构类型×最终判定"两维度逐一核实后（含读脚本源码，不只看文档描述），确认候选池并未扩大——但这个验证过程本身是必须做的，不能省略。判断标准：从历史调研里选N个信号做新一轮实验时，即使凭经验就能猜到大致该选哪几个，也要先让子agent产出全量候选清单并按同一套标准分类，用系统扫描的结果替代记忆，哪怕结论不变，也排除了"漏掉更好候选"的可能性。
- [信号组合方式要做全子集消融，不能只测"全部集成 vs 单一最强信号"两种]：第十二轮ML弱信号集成最初只测了"三信号等权集成"和"资金流单独使用"两种组合方式。后续补做全部7种非空子集（3单信号+3两两组合+1三者全部）的消融实验（`etf_rotation_v16_signal_combo_ablation.py`）才发现vol_ratio+flow二信号子集夏普（1.259）略高于三信号方案（1.235）。判断标准：候选信号数≤4时，全子集消融的计算成本很低，应该作为默认做法；子集间数值差距小（本例Δ0.024）时不必然要切换到局部数值最优——多重比较下"挑最优子集"本身有过拟合风险，其他指标（回撤）和样本量限制也要纳入最终决策，不是唯夏普论。
- [个体IC达标不代表组合层面有增量贡献，个体IC未达标也不能简单类推组合层面结论]：第十三轮两融余额行业信号（margin_balance）独立IC检验达标（+0.0414，年度同向占比87.5%），但并入现有3信号做4信号15子集消融后，含它的最优子集夏普（1.215）反而低于不含它的现有方案（1.235）——个体有效不代表对已有组合有增量贡献。同一轮里，另两个未通过IC初筛的信号（moneyflow_ratio/rate_beta）被追问"是否可能像第十二轮的flow一样有隐藏组合价值"，补做6信号63子集全量消融后证明它们在63种组合里从未进入最优，确认IC初筛排除是对的——但这个验证过程本身必须做，不能凭第十二轮flow的先例直接类推"未达标的可能有用"或者反过来直接类推"未达标的必定无用"，两种可能都存在，只有实测组合消融才能判断。详见`a_stock/docs/research.md`第十三轮小节，脚本`etf_rotation_v17_new_signal_ic.py`/`v18_signal_ablation.py`/`v19_full_signal_ablation.py`。
- [全子集消融找到的"最优"子集即使全样本三指标同时占优，也要做分年度拆解排除时间结构性衰减]：第十三轮63子集消融找到全局最优`crowding+flow+margin_balance`（夏普1.270/年化24.8%/回撤-23.1%，三指标同时优于现有方案的1.235/24.1%/-24.8%），看起来应该切换。但分年度拆解（`etf_rotation_v20_yearly_breakdown.py`）发现优势集中在2020(+0.151)/2023(+0.163)两年（合计+0.314），最近2025(-0.210)/2026(-0.103)系统性跑输（合计-0.313），几乎完全抹平全样本优势，判定为特定历史区间红利而非持续alpha，最终不采用。判断标准：搜索空间越大（本例63种远超第十二轮7种），找到的数值最优越需要分年度/分阶段验证——仅看全样本汇总指标会掩盖"优势来自历史特定区间、最近已失效"的情况，可能导致用衰减中的方案替换正在生效的方案。
- [估算"信号衰减速度/相关性"要用真实日期精确计算，不能用月内近似位置代替]：诊断ETF轮动调仓锚点敏感性原因时，最初为了估算"锚定日1 vs 锚定日25"两个信号的相关性，图省事用"月内第0个交易日 vs 第17个交易日（近似月末）"代替，算出相关性0.165（接近噪音），据此推断"混入的是接近噪音的信号"。但"锚定日25"和"上月锚定日25"是两个不同月份的日期，用户追问"不是相隔5日么"后精确核对真实日期，发现"本月1日"与"上月25日"实际交易日间隔平均只有4.6天，真实排名相关性是0.787（信号仍然有效），彻底推翻了之前的解释。判断标准：任何"相隔N天信号衰减到什么程度"的估算，如果涉及跨月/跨周期边界，必须用实际日期算交易日间隔重新验证，不能用"月内第几个交易日"这类近似位置代替真实的时间差，两者在边界附近可能对不上。
- [多信号加权融合前要先测试"仅替换执行时点"的滞后信号，隔离"信号质量"与"信号新鲜度"两个变量]：测试"本月1日得分+上月25日得分加权"是否优于纯本月1日基线，最初解释效果变差是"混入了接近噪音的旧信号"（基于上一条已被推翻的错误诊断）。用户质疑后重新验证：上月25日信号排名相关性0.787不算低，用它单独算分、但仍在本月1日执行（隔离"哪天打分"与"哪天执行"两个变量），夏普0.625，明显高于"打分和执行都在上月25日"的0.540，证明这个信号本身不是噪音。但依然明显低于纯本月1日基线的0.934——真正原因是"新鲜度"而非"质量"：本月1日的动量得分比上月25日多看了4-5个交易日最新价格，这几天的增量信息对预测未来收益有实质边际价值，加权引入旧数据等于稀释了这部分新鲜度，混入比例越高（w越低）夏普越单调下降。判断标准：任何"用两个不同时点的信号加权融合"的方案，效果变差时要先分别测"仅换算分时点，执行时点不变"和"打分与执行时点都换"两种滞后信号，才能判断是信号本身质量差还是单纯滞后损失了新鲜度信息，不能直接归因于"混入噪音"。
- [跨轮次汇总优化历史时，"两个方向各自都比基线好"不代表"两个都被采纳/叠加"，且不同小节的"基线"可能是不同时间区间]：给用户口头总结ETF轮动全部尝试时，把第一轮独立测试的"波动率反比加权仓位"(0.66→0.68)说成已经上线，实际当时只是与"风险调整动量"分别对比同一个等权基线各自测试，从未叠加验证，最终只上线了效果更好的风险调整动量，仓位层面维持等权（`signal_today.py`打印文案和`etf_rotation.py`的`use_ivol_weighting`参数默认值均可验证）。同一次总结还把第十二轮ML弱信号集成表格里的"基线夏普1.019"直接与开头提到的"当前上线全样本夏普1.053"并列对比，但1.019是纯动量策略在2019-2026子区间（对齐`fund_share`数据覆盖起点）重新计算的基线，1.053是2016-2026全样本基线，两者时间区间不同不可直接比较（脚本`etf_rotation_v15_weak_signal_ensemble_backtest.py`第231-239行两个版本用同一份截取区间的`close`/`rebal_dates`，1.019 vs 1.235才是可比的）。判断标准：口头/文档总结多轮调研结论时，"每行结论后面对应的具体数字用的是哪个基线、哪个时间区间"要逐条去源头脚本或表格上下文核对，不能因为"看起来都叫基线"就默认同一个数，也不能因为"两个方向都测过且都有效"就默认已经组合上线，需要用户追问才发现这两处偏差。
- [项目CLAUDE.md里不该写"第N轮/已测N个方向"这类会随每次调研递增的具体进度数字]：`a_stock/CLAUDE.md`策略方向表格里写着"探索性研究已收尾（十三轮61个方向）"，07-14又完成了锚点敏感性+加权融合测试，这个数字当天就过时，且`file-boundaries.md`本身规定"跨会话的研究结论/项目状态"只能存在于`memory/`，不能写进CLAUDE.md——写了之后每次调研进度变化都要多改一处，正是过去vectorbt描述"改一处漏三处"的同类问题模式，只是换了个位置发生。已把该行改为"详细进度见memory"，只保留稳定不变的定性状态（已上线/遇到较大困难）。判断标准：任何"第几轮/已测几个方向/累计N次"这类会随研究推进单调递增的计数，只应出现在memory的project_*.md里，CLAUDE.md和docs/*.md的总览性文字只写不需要频繁更新的定性描述，避免过时来源需要跨文件同步。
- [文档收尾时引用脚本文件名要实际核对，不能凭对话记忆写]：写"本月1日+上月25日加权测试"小节收尾句时，凭记忆把加权测试脚本写成`etf_rotation_v21_fixed_calendar_days.py`（实际那是纯锚点敏感性测试脚本），真正的加权测试在依赖v21函数的独立脚本`etf_rotation_v22_score_blend.py`里，被同一会话里的后续检查发现并纠正。判断标准：任何"脚本详见 xxx.py"这类收尾引用，写之前要用`ls`/`grep`核实文件名确实存在且对应该功能，不能仅凭对话过程中的印象直接写文件名。
- [`run_in_background`参数已经负责后台化，命令自己不能再加`&`]：标的池选择偏差实测里两次用Bash工具跑长任务，命令本身写成`python xxx.py > log 2>&1 &`（自带`&`），同时又传了`run_in_background: true`。自带的`&`让外层shell立即返回，工具把这个"瞬间返回"误判为任务已完成（exit code 0），实际Python进程早已脱离追踪，只能靠`ps aux`手动核实真实状态、用`sleep`轮询确认进度。正确做法：命令本身写成前台形式（不加`&`/`nohup`），完全交给`run_in_background`参数处理，这样任务会被正常追踪，完成时能收到`<task-notification>`。判断标准：用Bash工具的`run_in_background: true`时，command参数里不要再手动加`&`或`nohup ... &`这类自后台化写法，两者选一个即可，同时用必然导致追踪失效。
- [项目用venv时，Bash工具默认PATH下的`python3`可能不是项目环境]：本次实测用`sys.path.insert`引入`tushare`依赖的脚本直接用系统`python3`跑，报`ModuleNotFoundError: No module named 'tushare'`——项目根目录下存在`venv/`但当前shell没有激活它。用`venv/bin/python`（绝对路径）跑才成功。判断标准：项目根目录下如果存在`venv/`且用户此前提到过它是回测运行环境，跑`.py`脚本前先确认是否需要用`venv/bin/python`而非裸`python3`/`python`，尤其是新建脚本第一次运行时，报`ModuleNotFoundError`时先检查是否用错了解释器，不要假设系统Python已装好所有依赖。
